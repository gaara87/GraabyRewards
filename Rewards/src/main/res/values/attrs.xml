<resources>

    <!-- Declare custom theme attributes that allow changing which styles are
         used for button bars depending on the API level.
         ?android:attr/buttonBarStyle is new as of API 11 so this is
         necessary to support previous API levels. -->
    <declare-styleable name="GraabyTheme"></declare-styleable>
    <attr name="listPreferredItemPaddingStart" format="dimension" />
    <attr name="listPreferredItemPaddingEnd" format="dimension" />
    <attr name="primaryBackgroundLight" format="color" />
</resources>
