<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_menu_item_refresh"
        android:icon="@drawable/ic_action_refresh"
        android:menuCategory="container"
        android:title="Refresh"
        app:showAsAction="never"/>
    <item
        android:id="@+id/action_menu_item_settings"
        android:icon="@drawable/nav_settings"
        android:menuCategory="container"
        android:title="Settings"
        android:visible="false"
        app:showAsAction="never"/>

    <item
        android:id="@+id/action_menu_item_logout"
        android:menuCategory="container"
        android:title="Logout"
        app:showAsAction="never"/>


    <item
        android:id="@+id/action_menu_item_nfc_toggle"
        android:title="Enable NFC"
        android:visible="false"/>

</menu>